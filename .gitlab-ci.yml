image: gradle:8.13-jdk21-corretto

stages:
  - build
  - test
  - deploy_dev
  - deploy_prod

test:
  stage: test
  image: docker:24.0.5
  services:
    - docker:24.0.5-dind
  script:
    - docker info
    - docker run hello-world